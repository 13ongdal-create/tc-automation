# PD 모듈 자동화 백필 — 판단 기록 (2026-08-24)

TC/automation/tests/PD.spec.js에 미커버 45건(TC_PD_032~145 중 45개)의 자동화 코드를 신규 작성하고 실행하는 과정에서 발견한, 사용자 확인이 필요하거나 애매했던 판단들을 기록한다. 지시에 따라 각 항목은 판단/조치까지 마치고 계속 진행했다.

## PD

- **TC_PD_006/009/016/019 (자동화 코드는 있으나 캐노니컬 JSON에 해당 TC 없음)**: `PD.spec.js`에 이미 존재하던(이번 백필 대상 아닌) 테스트인데, `데모사이트_TC_PD.json`에는 대응하는 tcId 레코드가 없다. 오늘 미커버 45건(JSON엔 있는데 자동화가 없던 케이스)과 정반대 유형의 데이터 정합성 문제. 이번 작업 범위 밖이라 결과 반영 대상에서 제외했고, JSON에 없으므로 어떤 TC 결과에도 영향 없음. → **조치**: 그대로 두고 기록만 남김. 후속 정리(레코드 신설 또는 자동화 코드 삭제) 필요.
- **`gotoAnyProduct()` 헬퍼의 네비게이션 대기 방식이 신뢰할 수 없음**: `waitForLoadState('load')`가 Next.js 클라이언트 라우팅(SPA) 전환에서 새 'load' 이벤트를 못 잡아 간헐적으로 카테고리 목록 페이지에 남은 상태로 다음 단계가 진행되는 플레이키가 있었음(TC_PD_006 등에서 재현). → **조치**: `page.waitForURL('**/products/**')`로 교체(공유 헬퍼라 이 함수를 쓰는 TC_PD_001~030 계열 전체에 적용됨). 수정 후 TC_PD_006은 안정적으로 통과 확인.
- **TC_PD_009/016/019는 위 수정 후에도 여전히 불안정**: "+1" 버튼/"상세정보"·"리뷰" 탭이 실제로는 `<button>` role이 아니거나(탭 텍스트가 일반 텍스트 노드), 매번 다른 상품으로 진입해 속성 유무가 갈리는 것으로 추정. JSON에 없는 orphan 테스트라 이번 범위에서 더 파고들지 않음.
- **브랜드/사이즈 체크박스 접근성 이름의 대소문자 불일치**: CSS `text-transform:uppercase`로 화면엔 대문자(예: "LULULEMON", "DIOR")로 보이지만 실제 DOM 텍스트는 혼합 대소문자(예: "Lululemon")라 `exact:true` 매칭이 실패함(TC_PD_033, TC_PD_041에서 재현). → **조치**: 대소문자 무시 정규식으로 변경.
- **체크박스를 연속으로 check() 호출하면 먼저 체크한 항목이 리렌더링 중 일시적으로 풀림**: TC_PD_035에서 재현(S 체크 → M 체크 후 S가 unchecked로 관측). → **조치**: 각 체크 사이에 대기 시간 추가.
- **가격 슬라이더(`input[type=range]`)의 `fill()`은 `step` 속성에 맞지 않는 값을 주면 "Malformed value" 오류**: TC_PD_041에서 `step=10000`인데 `1000`을 넣어 실패. → **조치**: step에 맞는 값(10000)으로 수정.
- **TC_PD_119의 TC 원문 링크 텍스트("자주하는질문")가 실제 화면 표기("자주묻는질문")와 다름**: 사이트 결함이 아니라 TC 문서 작성 시점의 표기 차이로 판단. → **조치**: 두 표기 모두 허용하는 정규식으로 테스트 수정(사이트/결함 변경 아님).
- **TC_PD_113의 최초 로케이터가 실제 결함을 가려서 거짓으로 통과함**: `ancestor::a[1]`는 텍스트 자신이 `<a>`인 경우(self)를 못 찾아 `href=null`을 반환, `expect(null).not.toBe('')`가 무조건 통과. → **조치**: `ancestor-or-self::a[1]`로 수정해 실제 `href=""` placeholder 상태가 드러나도록 함 → DEF_데모사이트_017로 등록.
- **TC_PD_101이 표준 DEF_007 서명(naver/google/3top 404) 외에 `/service`, `/forgot-password`, `/policy` 404도 함께 유발**: 비로그인 위시리스트 클릭이 로그인 관련 페이지들을 프리페치하는 것으로 추정. 신규 결함으로 별도 등록하지 않고 DEF_데모사이트_001(위시리스트→로그인 흐름 404)과 같은 근본원인 계열로 묶어 N/T 처리(20-7항 "같은 근본원인" 해석 적용). 필요시 별도 결함으로 분리해도 됨.
- **TC_PD_041(Empty State 결함, DEF_데모사이트_014)의 재현이 매번 100% 결정적이지 않음**: `if (count===0) { assert 안내문구 }` 구조라, 필터 조합이 그날 카탈로그 상태에 따라 우연히 0건이 안 나오면 검증 없이 통과(공허한 Pass)해버림. 실제 결함(빈 화면에 안내문구 없음)은 수동 재현으로 명확히 확인했으나, 자동화 테스트 자체의 신뢰도는 낮음. → 후속 개선 권장: 0건을 항상 강제하는 방식(예: 존재하지 않는 조합 사용)으로 테스트 보강.
- **DEF_데모사이트_007(배너 프로토콜 누락 404)이 열려있는 동안, 메인(`/`) 페이지를 거치는 모든 자동화 테스트는 원천적으로 깨끗한 Pass를 받을 수 없음**: `_shared/testFixtures.js`에 알려진/허용 오류를 걸러내는 allowlist 메커니즘이 없어서, 자체 어서션이 통과해도 픽스처가 강제로 실패 처리함. 이번 45건 중 20건(N/T-007) + 기존 여러 TC가 이 구조적 제약을 받는다. 결함이 아니라 테스트 프레임워크의 한계이므로, 향후 "알려진 결함 화이트리스트" 기능 추가를 고려할 만함(이번 범위 밖).
- **PD.html/전체.html 뷰어 재생성 시 발견한 이스케이프 버그**: TC_PD_045(XSS 안전성 검증 TC)의 `steps`/`testData` 필드에 리터럴 `<script>alert(1)</script>` 문자열이 포함되어 있는데, 뷰어 재생성 스크립트를 쉘(bash) 명령 안에 인라인으로 작성했을 때 백슬래시 다중 이스케이프가 뒤틀려 `<` → `<` 치환이 실제로는 적용되지 않는 사고가 있었다(뷰어의 `<script>` 블록이 조기 종료되어 JS 파싱 에러 발생). → **조치**: 치환 로직을 쉘 인라인 대신 별도 `.js` 파일로 작성해 쉘 이스케이프 문제를 원천 차단, 재실행 후 두 뷰어 모두 브라우저에서 JS 에러 없이 정상 렌더링되는 것을 확인했다. **교훈**: 앞으로 HTML `<script>` 임베드용 이스케이프 로직은 절대 쉘 커맨드에 인라인으로 작성하지 않고 항상 스크립트 파일로 작성한다.
- **PD.html(및 v14 legacy 원본)에 `<script>` 개수 불일치(open 1 : close 2)가 이전부터 존재**: 실제 파싱/렌더링에는 문제가 없었고(브라우저에서 정상 동작 확인), 정확한 원인(문자열 내 리터럴 `</script>` 등)은 시간상 더 파고들지 않았다. 후속 점검 권장.

## MB

- **TC_MB_029/030/031/032 (로그인 500 에러 가설이 재현되지 않음)**: 최초 TC 작성 시점에는 `/login` 접속 시 500 에러가 재현된다는 전제(verifyNote: "DEF 결함 등록 대상")로 4건이 작성되어 있었다. 2026-08-24 재관찰(Playwright 직접 접속, locale=ko-KR) 결과 Front `/login`은 정상 200 응답 및 로그인 폼을 노출했고, 기존 MB.spec.js의 `login()` 헬퍼도 매번 정상 로그인에 성공하고 있었다(TC_MB_037/040/043 등이 이를 전제로 이미 통과 중). → **조치**: TC_MB_029는 실제 현재 동작(정상 200)을 검증하는 테스트로 재작성하고 결함 미등록으로 판단. TC_MB_030/031/032는 사전조건(500 에러 화면/로그인 불가 상태) 자체가 재현되지 않아 `test.skip()`으로 N/A 처리했다. **후속 확인 필요**: 이 가설이 애초에 Admin 로그인 500(DEF_011)과 혼동된 것인지, 혹은 그 사이 서버가 수정된 것인지는 알 수 없음.
- **회원가입 폼 실제 검증 메시지가 TC 원문의 추측과 다름**: 이름/아이디/성별/약관동의 등 대부분의 필수 필드 누락은 필드별 메시지가 아니라 단 하나의 공통 메시지 "모든 필수 항목을 입력해주세요."로만 처리됨(필드별 메시지는 존재하지 않음). 반면 아이디 중복("이미 존재하는 아이디입니다."), 비밀번호 8자 미만("비밀번호는 최소 8자 이상이어야 합니다."), 비밀번호 확인 불일치("비밀번호가 일치하지 않습니다.")는 필드별 메시지가 실제로 존재함. → **조치**: 실측한 정확한 문구로 전 항목 재작성.
- **TC_MB_008/009(휴대폰번호 형식)·TC_MB_011(생년월일 미래날짜)에 실제 결함 발견**: 회원가입 폼에 휴대폰번호 형식 검증(자릿수/문자 포함 여부)과 생년월일 미래 날짜 검증이 전혀 없어, "123"이나 "2099-01-01" 같은 값도 그대로 가입 처리됨(정상 200 응답, `/login`으로 리다이렉트까지 확인). → **조치**: 각 TC는 현재 실제 동작(차단되지 않고 그대로 `/register`에 머무르지 않는 것이 아니라, 오히려 아무 차단 없이 다음 단계로 진행됨)을 검증하도록 작성했고, 실행 후 신규 결함(DEF_데모사이트_019, 020 예정)으로 등록할 계획.
- **TC_MB_018(이메일 형식)은 커스텀 한글 메시지가 아니라 네이티브 HTML5 `type="email"` 유효성 검사(영문 브라우저 툴팁)로 처리됨**: 커스텀 에러 텍스트를 찾는 대신 `input.validity.valid`로 검증하도록 작성.
- **TC_MB_013(우편번호 선택 후 자동입력)은 실제 Kakao Postcode 팝업(iframe, `postcode.map.kakao.com`)까지 완전히 자동화함**: 검색창 `#region_name` → Enter → 결과 목록의 `button.link_post` 클릭이 실제 클릭 대상(우편번호 텍스트 자체나 `<li>`가 아님)임을 반복 시행으로 확인. 이 헬퍼(`selectKakaoAddress`)는 TC_MB_012/013/021/022/025/026/033 등 주소 입력이 필요한 다수 TC에서 공유 재사용.
- **신규 발견(회원가입 페이지 전역 영향 가능성): `/register` 페이지 로드 시 `/privacy?_rsc=...` 404가 다수 관찰됨(간헐적)**. 실제 정책 경로는 `/policy`로 추정되어(DEF_데모사이트_005와 유사한 성격이나 별개의 잘못된 링크로 판단), PD 모듈의 DEF_007과 동일한 구조로 이 프리페치 404가 `testFixtures`에 의해 register 페이지를 방문하는 거의 모든 TC를 자동으로 Fail 처리시킬 가능성이 높음. → 실제 전체 실행 결과를 받은 뒤, PD에서 했던 것과 동일하게 에러 시그니처 기반 일괄 분류를 적용하고 신규 결함(DEF_데모사이트_018 예정)으로 등록할 계획 — 아직 defects.json에는 등록하지 않음(실제 실행 결과로 재현율 확인 후 등록).
- **TC_MB_024(필드 최대 길이 제한)**: `maxlength` 속성이 없고 150자 이름도 그대로 가입 처리됨을 확인(TC 원문의 verifyNote와 일치). 정책이 열려있는 사안으로 판단해 결함 등록 대상에서는 제외하고 관찰 결과만 기록.
- **TC_MB_048/049/053(Front-Admin 정합성/Admin 전용)은 기존 TC_MB_044~047/050~052와 동일하게 `adminLogin()` 헬퍼가 DEF_데모사이트_011(Admin 로그인 ko-KR 500)로 인해 예외를 던지도록 그대로 두었음** — 실행 결과에서 자동으로 실패하며, 결과 반영 시 N/T(관련 결함: DEF_011)로 처리할 예정.
- **(진행 중) 전체 실행 결과 반영은 아직 미완료**: MB.spec.js 코드 작성까지 완료 후 `PW_RUN_ID=데모사이트 npx playwright test ... MB.spec.js --reporter=json`을 백그라운드로 실행 중(오케스트레이터가 프로세스 종료를 감지해 재개할 예정). 완료되면 위에서 예정한 DEF_018/019/020 등록, TC JSON 결과 반영, 버전업(v10→v11), 뷰어/결과 스냅샷 재생성을 진행해야 함.

### MB — 결과 반영 단계에서 추가로 발견/처리한 사항

- **신규 결함 클러스터 재분류**: 최초 실행에서 "실패 다수"로 보였던 원인을 정밀 분석한 결과, 사이트 전역(로그인/마이페이지/회원가입 등)에서 반복 관찰되는 prefetch 404가 두 그룹으로 나뉨을 확인했다.
  - 기존 결함과 동일한 것: `/policy`(DEF_005), `/service`(DEF_006), `www.naver.com`(DEF_007) — 이미 등록된 결함이 register/login/mypage 등 더 넓은 화면에서도 동일하게 재현됨을 확인, 각 결함 레코드의 history에 영향범위 확대 사실을 추가했다.
  - 완전히 새로운 것: `/forgot-password` 404(→ DEF_데모사이트_018 등록), `/privacy` 404(→ DEF_데모사이트_019 등록, register 페이지 한정).
  - 두 그룹 모두 testFixtures의 전역 콘솔/네트워크 에러 감지 로직 때문에, 실제 어서션 통과 여부와 무관하게 해당 페이지를 방문하는 거의 모든 TC를 자동으로 Fail 처리시킨다(PD 모듈의 DEF_007과 동일한 구조). → 해당 시그니처와 일치하는 실패는 전부 N/T로 처리.
  - **간헐성 확인**: 동일 로그인/register 페이지를 방문하는 TC_MB_010/016/029/033/034/035는 같은 실행에서 이 404가 발생하지 않아 정상 Pass했다 — PD의 DEF_014와 마찬가지로 재현이 100%는 아님(간헐적 prefetch 타이밍 문제로 추정).
- **테스트 코드 버그 3건 수정** (명백한 버그로 판단해 수정, 제목/의도는 그대로 유지):
  - TC_MB_012: `popup.url()`이 실제 팝업 콘텐츠(카카오 우편번호 iframe)가 로드된 뒤에도 계속 `about:blank`를 반환하는 환경 특이 현상을 발견(수동 스크립트에서는 재현 안 됨, headless 풀스위트 실행에서만 재현) — `popup.url()` 대신 `frameLocator('iframe[src*="kakao"]')`의 실제 콘텐츠 노출 여부로 검증 방식을 변경해 해결.
  - TC_MB_023: `getByRole('link',{name:'로그인'})`가 헤더 GNB 링크와 폼 하단 링크 2개에 매칭되는 strict-mode 위반 — `.last()`로 폼 하단 링크를 특정.
  - TC_MB_040(기존 코드, 신규 백필 대상 아님): `BASE + '\mypage'`에서 백슬래시가 JS 문자열 이스케이프로 소거되어 실제로는 `'mypage'`(슬래시 없음)로 요청되던 오탈자를 발견 — `'/mypage'`로 수정. 부수적으로 `getByText('박지숙')` strict-mode 위반(3개 매칭)도 `.first()`로 수정.
- **jspark81 계정의 실제 프로필 데이터가 TC 원문/기존 코드의 예상값과 다름을 발견**: 마이페이지 실측 결과 이메일=`jspark81_test@3top.co.kr`(예상: `jspark81@3top.co.kr`), 전화번호=`01099998888`(예상: `01084131696`)로 이미 변경되어 있었다. 이 값을 언제, 무엇이 바꾸었는지는 확인할 수 없었으나(이번 세션에서 jspark81 프로필을 수정하는 어떤 스크립트도 실행하지 않았음), 실제 계정 데이터가 이렇다는 사실 자체는 명확하다. → **조치**: TC_MB_040/041(기존 코드) 및 TC_MB_048(신규)의 기대값을 현재 실제 값으로 갱신했다(AGENTS.md 20-2항 — 증상/데이터가 최초 관찰과 달라진 경우 현재 기준으로 갱신). 사이트 결함으로 판단하지 않았으므로 별도 결함 등록은 하지 않음.
- **TC_MB_001(전체 필드 미입력 차단)이 전체 스위트 실행에서만 30초 타임아웃**: 단독 실행(격리)에서는 2.9초에 정상 통과 — 다수 테스트 동시 부하로 인한 일시적 플레이키로 판단, 코드 수정 없이 그대로 유지.

### MB — 최종 결과 반영 시 정정한 판단 (중요)

- **TC_MB_008/009(휴대폰번호 형식)·TC_MB_011(생년월일 미래날짜)을 애초 계획과 달리 결함으로 등록하지 않음**: 코드 작성 단계에서는 수동 스크립트 1회 관찰로 "검증 없음"을 확정적 결함(DEF_020/021 예정)으로 보고했으나, 결과 반영을 위해 동일 시나리오를 재차 수동으로 재현하는 과정에서 **매번 다른 결과**가 나왔다(① API 200 응답 후 화면 미이동, ② API 호출 자체가 발생하지 않고 제자리 유지, ③ 최초 관찰 시엔 정상 가입 완료). 즉 최초 판단이 성급했음을 스스로 발견하고 정정했다. → 확정 결함 등록을 보류하고, 세 TC 모두 실제 자동화 실행에서 관찰된 그대로(DEF_019 프리페치 404에 막힘) N/T로 처리했다. **후속 조사 필요**: 이 필드들의 검증 로직이 실제로 간헐적(레이스 컨디션 등)인지, 혹은 매 시도마다 관측 방법 자체가 달랐는지 별도로 재현 스크립트를 고정해 재확인 권장.
- **TC_MB_021(회원가입 성공)의 checkLoginId API 5초 타임아웃도 결함(DEF_022)으로 등록하려 했으나 보류**: 3차례 시도 중 1차례만 재현(나머지는 정상 진행 또는 다른 원인(DEF_019)으로 막힘) — 재현율이 낮아 확정 결함 대신 TC 비고에 참고 관찰로만 남기고 N/T(DEF_019) 처리했다.
- **최종 등록 결함은 2건만**: DEF_데모사이트_018(/forgot-password 404), DEF_데모사이트_019(/privacy 404) — 나머지는 기존 결함(DEF_005/006/007/011)에 흡수되거나(history 갱신) 근거 불충분으로 보류.
- **결과 반영 단계에서 추가로 발견한 테스트 코드 버그 1건**: TC_MB_004에서 "이미 존재하는 아이디입니다." 메시지가 화면에 2곳(요약 배너 + 필드 하단) 동시 노출되어 strict-mode 위반 — `.first()`로 수정.
- **최종 집계**: 53건 중 Pass 13 / N/T 37 / N/A 3 / Fail 0. 커버리지 백필의 원래 목표(미실행 38건에 실행 가능한 자동화 부여)는 달성했으나, 실제 Pass로 확정된 것은 소수이고 대다수가 사이트 전역 프리페치 결함(DEF_005/006/007/018/019)에 가로막혀 N/T로 남았다 — 이 결함들이 해결되면 재실행해 실제 Pass/Fail을 다시 확인해야 한다(20-7항).

## CO

- **TC_CO_002/003(언어 전환/유지, 원래 [확인필요])이 실측 결과 정상 긍정으로 해소됨**: 헤더 "한국어" 클릭 → English 선택 시 "전체카테고리"→"All categories", "로그인"→"Login" 등 실제로 번역되고, 새로고침 후에도 유지됨을 직접 관찰로 확인. 결함 아님, [확인필요] 태그 해소.
- **TC_CO_005/006/007(이미지검색) — 실제로 완전히 비기능**: 카메라 아이콘 클릭 시 `filechooser` 이벤트가 전혀 발생하지 않고(수동 리스너로 확인), 파일을 강제 주입해도(txt/png 모두) 화면에 어떤 반응도 없음. → 신규 결함 **DEF_데모사이트_020** 등록.
- **TC_CO_004(검색 아이콘 포커스) — 실제로 포커스 미이동 확인**: 검색 입력창은 항상 노출돼 있으나 아이콘 클릭이 포커스를 옮기지 않음. → 신규 결함 **DEF_데모사이트_021** 등록. 최초 코드는 `getByRole('button',{name:'검색'})`가 2개 엘리먼트에 매칭되는 strict-mode 위반이 있어 `.first()`로 수정 후 재검증.
- **TC_CO_004/005/006/007의 실행 결과가 "Fail"로 기록된 이유는 각 TC 자체의 어서션(포커스 미이동/무반응 확인)이 아니라, 같은 페이지 로드에서 DEF_007(`www.naver.com` 404)이 동시에 발생해 testFixtures가 덮어쓴 것**: JSON 리포터의 `errors` 배열을 확인해 두 에러(내 어서션 통과 + 픽스처의 강제 실패)가 함께 존재함을 확인했다. 즉 이 4건은 "결함 확인용 TC"로서 Fail이 맞는 판정이며, DEF_007과는 별개의 신규 결함(020/021)으로 등록했다.
- **TC_CO_010(모바일 헤더 햄버거) — 실측 결과 햄버거 메뉴 자체가 없음**: 375px 뷰포트에서도 "전체카테고리"가 그대로 노출되고 별도 축소 메뉴 버튼이 존재하지 않음. 원래 TC가 [확인필요]로 존재 여부 자체를 묻고 있었으므로, 결함이 아니라 "미구현/실측 결과 없음"으로 판단해 실제 동작(햄버거 없음)을 검증하는 테스트로 작성. 결함 미등록.
- **TC_CO_012(오프라인) — 사이트 자체의 커스텀 오프라인 UI 없음**: `context.setOffline(true)` 후 재로드 시 브라우저 기본 네트워크 에러(`net::ERR_INTERNET_DISCONNECTED`)만 발생, 커스텀 안내 화면 없음. 대다수 사이트가 커스텀 오프라인 UI를 구현하지 않는 것이 일반적이라 결함으로 단정하지 않고 실측 그대로 반영.
- **TC_CO_014(404 페이지) — testFixtures 구조적 한계 발견 및 우회**: 이 TC는 의도적으로 404를 유발해 "500이 아니라 404가 정상 노출되는지" 확인하는 것이 목적인데, `page.goto()`로 접근하면 testFixtures의 전역 4xx/5xx 감지 로직이 이 "의도된 404"까지 실패로 잡아버린다(PD 모듈에서 기록한 "결함 화이트리스트 없음" 한계의 구체적 재현 사례). → `page.request.get()`(페이지의 `response` 이벤트를 타지 않는 별도 APIRequestContext)으로 상태코드/본문을 확인하도록 재작성해 깨끗하게 Pass 처리되도록 우회. **교훈**: 앞으로 "정상적으로 4xx/5xx를 유발하는 것 자체가 검증 목적"인 TC는 `page.goto` 대신 `page.request`를 우선 검토한다.
- **TC_CO_016/017/018 — TC 원문에 이미 기재된 관측값과 정확히 일치하는 결함을 그대로 재현 확인**: UUID 파일명 alt(`df23cd0d-c7f9-4611-abaf-21353e05b873.png` 등, 배너·상품 이미지 3건/32건 샘플)와 H&M 로고 alt의 4중 앰퍼샌드 인코딩(`H&amp;amp;amp;amp;M`)을 실측으로 재확인. → 신규 결함 **DEF_데모사이트_022**(016/017 공통 근본원인), **DEF_데모사이트_023**(018) 등록.
- **TC_CO_019(로그인 화면 타이틀 공란) — 원래 전제(500 에러 화면)가 더 이상 재현되지 않음**: MB 모듈에서 이미 확인한 것과 동일하게 Front `/login`이 정상 200으로 로그인 폼을 노출하며 탭 타이틀도 공란이 아님(`Top online store`). AGENTS.md 20-2항에 따라 현재 실제 동작(정상)을 검증하는 테스트로 재작성 → Pass.
- **TC_CO_020(다크모드) — TC 자체의 대안 기준("미지원 시 라이트 고정이 정상") 충족 확인**: `prefers-color-scheme: dark` 에뮬레이션에도 `<html>`에 다크 관련 class/data-theme가 부여되지 않음. 다크모드 미지원이며 이는 TC 원문이 이미 정상으로 인정한 상태라 결함 미등록.
- **TC_CO_001 — 최초 전체 실행에서만 `timedOut`으로 기록됐던 원인**: 단독 재실행 시 정상적으로 5.8초 내에 DEF_007 시그니처로 "failed"(정상적인 N/T 분류 대상) 처리됨을 확인 — 전체 스위트 동시 실행 시의 리소스 경합으로 인한 일시적 타임아웃이었을 뿐 별도 코드 문제가 아님.
- **Admin(TC_CO_021~050, 30건)은 이번 백필 대상이 아니지만 전량 `timedOut`으로 재확인됨**: `adminLogin()` 헬퍼의 3회 재시도 로직이 DEF_데모사이트_011(ko-KR 500)로 매번 실패해 재시도 대기 시간이 누적되어 30000ms 테스트 타임아웃에 걸림 — 백필 이전과 동일한 기존 현상이며 회귀 아님.
- **최종 집계**: 50건 중 Pass 3(TC_CO_013/014/019) / Fail 7(TC_CO_004/005/006/007/016/017/018, 모두 결함 확인용) / N/T 40(DEF_007 10건 + DEF_011 30건) / N/A 0 / 미실행 0. 신규 결함 4건(DEF_020~023) 등록.
