---
name: ecommerce-qa-tc-generator
description: 이커머스 서비스(네이버쇼핑 등)를 대상으로 정밀 QA 테스트케이스(TC)와 Interactive HTML 뷰어를 생성한다. 대상 URL/기획안/기존 TC 리스트를 제시하면 도메인 분석 → 시나리오 검토 → 승인 → TC 자체검증 → HTML 생성 순으로 진행하며, "추측 금지"와 "초보 테스터도 바로 따라할 수 있는 구체성"을 최우선 기준으로 삼는다.
---

# 이커머스 QA TC 생성 스킬

## 0. 기본 원칙 (Approval Protocol)

1. **[Phase 1~3: 사전분석 → 시나리오검토표 제출] → [사용자 승인] → [Phase 3.5: TC 자체검증(기준문서 있는 경우만)] → [Phase 4: HTML 생성]** 순서를 반드시 지킨다.
2. Phase 3에서 사용자와 확정한 범위(TC 건수/모듈)를 Phase 4에서 **임의로 확장하지 않는다.** 확장이 필요하면 반드시 Phase 3을 다시 제출해 승인받는다.
3. 확인되지 않은 정책/동작은 절대 임의로 "정상 동작함"이라 단정하지 않는다.

## 1. 기준문서 대조 절차 (★ 최우선 규칙)

| 상황 | 처리 |
|---|---|
| 기준문서(정책서/화면설계서/Figma/PRD/ERD) **없음** | 공개 화면 기준 역설계 분석 진행, §4 예외 처리(확인필요 태그) 적용 |
| 기준문서 **있음** | HTML 생성 **전** 아래 우선순위로 대조 검토 후 생성 |

기준문서 대조 우선순위(항상 이 순서로 점검):
```
1순위. 문서가 최신 버전인지 확인
2순위. 문서 내 삭제/수정/추가 사항 파악 → TC에 반영
3순위. 문서의 기준 정보에 TC가 부합하는지 최종 점검
```
- CSS/화면 관련 기대결과는 Figma·화면설계서가 있는 경우 **최신 버전 여부를 선(先) 확인** 후 재검토한다.
- 문서만으로 판단이 애매한 사항은 **HTML 생성 전 반드시 사용자에게 질의**하고 답변을 받은 뒤 생성한다. (임의 추정 금지)

## 2. 근거 원칙 — 추측 금지 (No Speculation) ★★★ 핵심

모든 TC는 아래 근거 중 하나 이상을 명시할 수 있어야 한다.

**✅ 허용된 근거**
- Figma frame 또는 캡처된 화면(실제 확인된 UI)
- PRD/ERD 섹션(Notion 원문에 명시된 내용)
- Slack 스레드 결정 사항
- ERD의 API spec(엔드포인트 + 필드)
- 메뉴트리(V3) 항목
- 기존 Regression TC 패턴(재사용)
- `skillset` 도메인 지식(명시된 것만)

**❌ 금지된 근거(추측)**
- "일반적인 UX 패턴"
- "다른 서비스는 이렇게 함"
- "이럴 것으로 예상됨"
- "스펙에 명시되지 않았지만 필요할 것 같음"
- "typical form pattern"

**⚠️ 예외 처리**: 근거가 불확실하지만 QA 관점에서 반드시 검증이 필요한 경우
1. TC 제목(테스트 항목) 앞에 `[확인필요]` 태그 추가
2. 기대결과에 "스펙 확정 시 재검증 필요" 명시
3. 우선순위를 **P3**로 강제 지정(스펙 확정 후 상향 조정)

## 3. TC 자체검증 체크리스트 (Phase 3.5 — 기준문서가 있는 경우에만 수행)

**적용 조건**: 이 단계는 §1의 기준문서(정책서/화면설계서/Figma/PRD/ERD 등)가 **존재하는 프로젝트에서만** 수행한다.

| 상황 | 처리 |
|---|---|
| 기준문서 **있음** | 아래 체크리스트를 Phase 3 승인 후 ~ Phase 4 착수 전에 전 TC에 대해 수행 |
| 기준문서 **없음** | 이 단계를 **생략**한다. 대신 §2의 예외 처리 원칙(근거 불확실 시 `[확인필요]` 태그 + P3 고정)을 TC 작성 시점에 기본 적용하는 것으로 갈음한다 |

기준문서가 있을 때, 각 TC 작성 후 아래 체크리스트를 **모두 통과해야 HTML 뷰어에 포함**한다.

```
[ ] 1. 이 TC의 진입 경로가 Figma/PRD에 존재하는가?
[ ] 2. Platform(App/Web)이 메뉴트리와 일치하는가?
[ ] 3. Out of scope에 포함되지 않는가?
[ ] 4. Procedure만 보고 QA가 즉시 실행 가능한가?
[ ] 5. Expected result가 검증 가능한 구체적 값/상태인가?
[ ] 6. 근거(Figma/PRD/ERD/Slack)를 댈 수 있는가?
```

- **하나라도 X**이면: 해당 TC를 사용자에게 체크리스트와 함께 제시하고, 질의를 통해 확인받은 뒤 **"TC 삭제"** 또는 **"[확인필요] 태그 부착(§2 예외 처리 적용)"** 중 하나로 처리한다.
- 결과를 간단한 검증 요약(통과/삭제/확인필요 건수)으로 사용자에게 보고한다.

## 4. 16개 컬럼 스키마

| No | 컬럼 | 작성 규칙 |
|---|---|---|
| 1 | TC ID | `TC_{모듈코드}_{순번}` (모듈별 Prefix 고정) |
| 2 | 대분류 | 상품/구매프로세스/클레임/회원/탐색/콘텐츠/품질·비기능/예외·회귀 등 |
| 3 | 중분류 | 모듈명(상품상세/장바구니/주문결제 등) |
| 4 | 소분류 | 세부 기능명 |
| 5 | 플랫폼 | **PC / APP / PC+APP 중 하나만 사용** (그 외 값 금지) |
| 6 | 유형 | UI/기능/API/성능/데이터 (5종 고정) |
| 7 | 테스트 항목(시나리오) | 한 줄 요약. 근거 불확실 시 `[확인필요] ` 접두 |
| 8 | 기준문서 | 기준문서가 있으면 `{파일명}_{버전명}_{해당기준이 정의된 페이지번호}` 형식. 없으면 `-` |
| 9 | 우선순위 | P1/P2/P3 (§2 예외 처리 대상은 P3 고정) |
| 10 | 사전조건 | §6 참조 |
| 11 | 테스트 수행 절차 | §6 참조 |
| 12 | 기대 결과 | §7 참조 |
| 13 | 실행결과 | Pass/Fail/Blocked/N/A (HTML에서 드롭다운) |
| 14 | 담당자 | 자유 입력 |
| 15 | 이슈내용 요약 | 자유 입력 |
| 16 | 이슈링크 | 자유 입력(Jira 등) |

## 5. 사전조건 작성 규칙 (★ 초보자 기준)

- **원칙: 기대결과를 얻기 위해 절차 수행에 실제로 필요한 항목만** 남긴다. 불필요한 나열 금지.
- 회원유형/계정 정보가 결과에 영향을 주지 않는 케이스(상품 데이터 정합성, 검색/리스트, 가격비교, 접근성, 크로스브라우저, 보안 취약점 점검, 성능 등)는 **계정 정보 자체를 넣지 않는다.**
  - 판단 기준: 모듈이 로그인/마이페이지/주문결제/취소반품/결제보안/정기구독/해외직구/프로모션에 해당하거나, 시나리오에 "로그인/비회원/멤버십/쿠폰/포인트/구매확정/동기화"가 언급될 때만 계정 정보 포함.
  - 남길 항목이 없으면 `특이 사전조건 없음` 한 줄로 표기.
- 계정 정보를 넣을 때는 **하나로 뭉치지 말고 세분화**하여 번호를 매긴다:
  ```
  1) Acc_Normal (네이버 통합회원)
  2) 결제수단 : 등록카드 1개 - 신한카드
  3) 보조결제수단 : 포인트 0P 보유
  4) 쿠폰 : 0개 보유
  5) 배송지 : 기본배송지 서울 강남구 등록
  ```
- 값이 "0개/0P/없음" 등 **기본값이고 해당 시나리오와 무관하면 그 줄 자체를 숨긴다.**
- 검증도구(개발자도구/프록시 등) 안내는 사전조건이 아니라 **절차(11번)로 이동**시킨다.
- "특이조건 :" 같은 라벨은 붙이지 않고 내용만 적는다.
- 항목이 2개 이상이면 절차와 동일하게 `1) 2) 3)` 넘버링한다.

## 6. 테스트 절차 작성 규칙 — TC 유형별 구체화 (★ 초보자 기준)

공통 원칙: 화면 진입 단계는 추상적으로 쓰지 않는다.
- 로그인 필요: `계정 로그인 후 {화면명} 진입`
- 로그인 불필요(비회원 등): `{화면명} 진입`
- "사전조건 계정으로 관련 화면 진입" 같은 모호한 문구 금지.

### 6-1. UI TC
- 진입 경로를 명확히 명시한다. 예: `"Trade > Perp > Positions 진입 → TP/SL Set 클릭"`
- 검증 요소는 구체적으로 쓴다: 버튼 라벨 원문, 색상(Hex), 위치, 문구 원문.

### 6-2. API TC
- 검증 도구를 플랫폼별로 명시한다.
  - Web: `"브라우저 개발자 도구 > Network 탭"`
  - App: `"Charles / Proxyman / mitmproxy 등 프록시 도구"`
- 확인할 request URL(엔드포인트 원문)
- 확인할 request body 필드(필드명 + 예상값)
- 확인할 response 필드
- HTTP status code 확인
- 개발자도구 진입 방법 자체는 절차의 별도 보조설명(`*`)으로 분리한다 (§ 8 참조).

### 6-3. Network(성능/예외) TC
- 재현 방법을 플랫폼별로 명시한다.
  - Web: `"DevTools > Network > Offline 체크"` / `"Throttling: Slow 3G"`
  - App: `"기내모드 ON"` / `"Wi-Fi 끄기"`
- 어느 요청이 실패하는지 실패 지점을 명시한다.
- UI 반응(예외 모달/Skeleton UI 등) 확인 방법을 명시한다.

### 6-4. 상태 전이 TC
- 초기 상태를 어떻게 만드는지 구체적 세팅 방법을 쓴다.
- 트리거 방법(어떤 동작으로 상태가 바뀌는지)을 쓴다.
- 최종 상태를 어떻게 검증하는지 방법을 쓴다.

### 6-5. 회귀 TC
- 기존 시나리오 재현 절차를 쓴다.
- 신규 기능 도입 후 변경 여부를 확인할 포인트를 명시한다.

### 6-6. 개발자도구/프록시 안내 축약 형식 (전체 유형 공통)
개발자도구·프록시 도구가 필요한 단계는 **핵심 동작 1줄(번호 있음) + 보조설명 1줄(번호 없음, `*` 접두)**로 분리한다.
```
1) 개발자도구 실행 후 상단의 'Network' 탭 선택
    * 화면에서 마우스 오른쪽 버튼 클릭 → '검사(Inspect)' 선택(또는 키보드 F12 키)
```

## 7. 기대결과 작성 규칙 (★ 구체적 값/상태 + 확인필요 분리)

- 본문은 "무엇이 검증되어야 하는가"를 **검증 가능한 구체적 값/상태**로 서술한다. 막연한 "정상 동작해야 한다" 수준의 서술은 지양하고, 가능한 한 필드명·예상값·상태값을 구체적으로 적는다.
- **Hex 코드, Bold, strikethrough, opacity% 등 CSS 상세 수치**는 본문에서 분리하여 하단에 `* ` 보조설명(빨간색)으로 별도 표기한다.
  ```
  구매 관련 버튼이 비활성화(Disabled) 상태로 노출되며, 클릭 시 "옵션을 선택해주세요" 안내 문구가 노출된다.
  * 비활성화 버튼 배경색 : Hex #D1D5DB   ← 빨간색
  ```
- **정책 미문서화로 확인이 필요한 사항**도 동일하게 본문에서 분리하여 `* ` 보조설명(빨간색)으로 표기한다. `[확인필요]` 배지는 사용하지 않되(§2의 제목 태그와는 별개), TC 제목에는 §2 규칙에 따라 `[확인필요]`를 붙인다.

### 좋은 API TC 예시 (참고용)
```
사전조건 & 절차:
1) Advanced TP/SL 모달에서 Input=ROI, ROI=-10 입력 후 Confirm
2) 개발자 도구 > Network 탭 > POST /api/v2/trade/one-way/partial-tpsl/{symbol} 요청 클릭
3) Payload 탭에서 request body 필드 확인

기대결과:
1~3. Request body:
     - side: "Long"
     - orderQty: "0.5" (문자열)
     - triggerPrice: "59400.0"
     - triggerDirection: "Down"
     - executionType: "Market"
     - roi/pnl 필드 없음 (client-side conversion 원칙)
     - orderTp*/orderSl* prefix 없음 (One-way는 flat 구조)
```
이 예시처럼 **필드명 + 예상값 + 원칙(왜 그런 구조인지)**까지 구체적으로 적는 것을 목표로 한다.

## 8. Phase 4 — Interactive HTML 뷰어 스펙

- **탭 UI**: 상단에 "전체 + 모듈별" 탭을 두어 TC 리스트를 모듈 단위로 필터링할 수 있게 한다.
- **테마 토글**: 다크/라이트 모드 실시간 전환.
- **KPI 카운터**: 전체/P1/P2/P3.
- **모듈별 실행현황 통계표**: Pass/Fail/Blocked/N/A/미실행 건수와 함께 **수행율(=(Pass+Fail+Blocked+N/A)/전체), Pass율(=Pass/수행건수), Fail율(=Fail/수행건수)**을 모듈별로 집계하는 표를 KPI 카운터 **상단**에 배치, 실행결과 변경 시 실시간 갱신.
- **CSV 다운로드 버튼**: 현재 필터링된(또는 전체) TC 리스트를 CSV로 다운로드할 수 있는 버튼을 제공한다. 16개 컬럼(실행결과/담당자/이슈내용/이슈링크 포함) 전체를 내보낸다.
- **테이블 가로 스크롤 금지**: `table-layout:fixed` + 컬럼별 % 폭 고정 + 텍스트 줄바꿈(`word-break`)으로 페이지 폭에 맞춘다. 세로 스크롤도 제거(전체 TC가 페이지 스크롤로 펼쳐지게).
- **모달 2종**: 테스트 계정 매트릭스, User Flow Map(회원유형별 5단계는 **표 형식**으로 제시).
- 13~16번 컬럼(실행결과/담당자/이슈내용/이슈링크)은 브라우저 메모리에만 저장됨을 하단에 명시한다(새로고침 시 초기화, CSV 다운로드로 백업 가능함을 함께 안내).

## 9. HTML/JS 생성 시 필수 버그 방지 규칙 (★ 중요)

과거 이스케이프 처리 오류로 스크립트 전체가 깨져 TC가 0건으로 렌더링된 사고가 있었다. 이를 방지하기 위해:

1. **인라인 `onclick`/`onchange` 문자열 조합 금지.** 반드시 `data-id` 속성 + `addEventListener` 방식(이벤트 위임)으로 구현한다.
2. HTML 생성 후 **`node --check`로 JS 구문 검증**을 반드시 수행한다.
3. 가능하면 **jsdom으로 런타임 시뮬레이션**(탭 클릭, 필터, 실행결과 변경, CSV 다운로드 트리거 등)까지 수행하여 실제 동작을 확인한 뒤 사용자에게 전달한다.
4. "확인은 했으나 검증하지 못한 부분"과 "확인한 부분"을 답변에서 명확히 구분해 보고한다.

## 10. 진행 시 커뮤니케이션 원칙

- 매 단계 시작 전 "이번 작업에서 진행할 내용"을 먼저 안내한다.
- 표/체크리스트 우선, 장문 설명 지양.
- 산출물 생성 후에는 근거(무엇을 검증했는지)를 함께 제시한다.
- 대규모 확장(예: TC 건수 증가)은 반드시 재승인을 받는다.
- Phase 3.5(자체검증)에서 삭제/확인필요 처리된 TC가 있으면, HTML 생성 결과 보고 시 함께 요약한다.
